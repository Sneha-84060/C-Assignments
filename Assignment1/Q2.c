#include<stdio.h>
int main()
{
int a;
    printf("Enter a no.:");
    scanf("%d",&a);

printf("%d in character:%c\n %d in decimal:%d\n %d in octal:%o\n %d in hexadecimal:%x\n",a,a,a,a,a,a,a,a);

return 0;
}
